{% extends '::base.html.twig' %}
{% block body %}

    <section class="flashbag">
        {% for type, flashMessages in app.session.flashbag.all() %}
            {% for flashMessage in flashMessages %}
                <div class="alert alert-{{ type }} fade in">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <strong>info !</strong>  {{ flashMessage|trans({}) }}
                </div>
            {% endfor %}
        {% endfor %}
    </section>



        <div class="container">
        <div class="row col-md-6 col-md-offset-2 custyle">
            <div class="alert alert-danger alert-message" role="alert">
                <span class="glyphicon glyphicon-remove"></span>
                <p>
                    <b><h3>{{ count}}</h3></b>
                </p>
            </div>
            <table class="table table-striped custab">
                <thead>
                <a href="{{ path('getdata') }}" class="btn btn-primary btn-xs pull-right">Add new categories</a>


                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Paciente</th>
                    <th>Info</th>




                    <th class="text-center">Action</th>
                </tr>
                </thead>


                 {% for d in data %}
                <tr>
                    <td>{{ d.id }}</td>
                    <td>{{ d.title }}</td>
                    <td>{{ d.description }}</td>
                    <td>{{ d.pacient }}</td>
                    <td>{{ d.info }}</td>



                    <td class="text-center"><a class='btn btn-info btn-xs' href="{{ path('update',{'id':d.id}) }}"><span class="glyphicon glyphicon-edit"></span> Edit</a> <a href="{{ path('delete',{'id':d.id}) }}" class="btn btn-danger btn-xs"><span class="glyphicon glyphicon-remove"></span> Del</a></td>
                 {% endfor %}
                     </tr>


            </table>
        </div>
    </div>
    <button class="buton" value="add">click modal</button>





{% endblock %}
{% block javascripts %}
    <script>



    </script>
{% endblock  %}